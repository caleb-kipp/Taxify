name: CD-Prod
on:
  workflow_dispatch:

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Confirm deploy
        run: |
          echo "Manual production deploy started"
      - name: Build and push images
        run: |
          echo "Build & push images to production registry"
      - name: Apply k8s manifests
        run: |
          echo "kubectl apply -f k8s/overlays/prod"